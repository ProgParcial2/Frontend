name: progparcial2_frontend
description: Aplicación Flutter para consumir el backend del Segundo Parcial
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🔹 Navegación declarativa moderna
  go_router: ^14.0.2

  # 🔹 Manejo de estado
  provider: ^6.1.2

  # 🔹 Peticiones HTTP
  http: ^1.2.2

  # 🔹 Manejo de fechas (opcional pero útil)
  intl: ^0.19.0

  # 🔹 Para mostrar íconos de Material Design
  flutter_svg: ^2.0.10+1

  # 🔹 Core Flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
